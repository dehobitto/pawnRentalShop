@model RentFormViewModel
@{ ViewData["Title"] = "New Rental"; }

<div class="row justify-content-center">
<div class="col-md-6">

<h2 class="mb-4">New Rental</h2>

<div class="card shadow-sm">
<div class="card-body">

<form asp-action="Rent" method="post">
    @Html.AntiForgeryToken()

    <div class="mb-3">
        <label asp-for="Sku" class="form-label fw-semibold"></label>
        <select asp-for="Sku" asp-items="Model.Items" class="form-select">
            <option value="">— select an item —</option>
        </select>
        <span asp-validation-for="Sku" class="text-danger small"></span>
    </div>

    <div class="mb-4">
        <label asp-for="Days" class="form-label fw-semibold"></label>
        <input asp-for="Days" type="number" min="1" max="365" class="form-control" />
        <span asp-validation-for="Days" class="text-danger small"></span>
    </div>

    <div class="d-flex gap-2">
        <button type="submit" class="btn btn-primary">Process Rental</button>
        <a asp-controller="Item" asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
    </div>
</form>

</div>
</div>

</div>
</div>
